name: Benchmarks

# DISABLED: Benchmarks workflow is a placeholder - no Rust binaries or bench targets exist yet
# Re-enable when:
# 1. Rust workspace with Cargo.toml is committed to examples/
# 2. Makefile has bench-all and bench-regression targets
on:
  workflow_dispatch:  # Manual trigger only for now
  # Disabled triggers:
  # push:
  #   branches: [main]
  # pull_request:
  #   branches: [main]
  # schedule:
  #   - cron: '0 0 * * 0'

jobs:
  benchmark:
    name: Run Benchmarks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv venv
          source .venv/bin/activate
          uv pip install -e ".[dev]"

      - name: Build Rust workspace (manual implementations)
        run: |
          # Build the Rust workspace which contains all manual Rust implementations
          # These are guaranteed to work, unlike depyler-generated code
          cd examples && cargo build --release

      - name: Run benchmarks
        run: |
          source .venv/bin/activate
          make bench-all

      - name: Check for regressions
        id: regression_check
        continue-on-error: true
        run: |
          source .venv/bin/activate
          make bench-regression

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: benchmark-results
          path: benchmarks/reports/*.json

      - name: Comment regression status on PR
        if: github.event_name == 'pull_request' && steps.regression_check.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '⚠️ **Performance Regression Detected**\n\nPlease review the benchmark results in the workflow artifacts.'
            })

  benchmark-summary:
    name: Benchmark Summary
    runs-on: ubuntu-latest
    needs: [benchmark]
    if: always()

    steps:
      - name: Summary
        run: |
          echo "📊 Benchmark Summary"
          echo "==================="
          echo ""
          echo "Benchmark Status: ${{ needs.benchmark.result }}"
          echo ""
          if [ "${{ needs.benchmark.result }}" == "success" ]; then
            echo "✅ All benchmarks completed successfully"
            echo "✅ No performance regressions detected"
          else
            echo "⚠️  Check benchmark job for details"
          fi
          echo ""
          echo "Benchmark Infrastructure:"
          echo "  - Native binary benchmarks (bashrs)"
          echo "  - Docker container benchmarks"
          echo "  - Performance regression detection"
          echo "  - Statistical analysis (10 iterations, 3 warmup)"
